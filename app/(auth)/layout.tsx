/**
 * Owner auth (login / signup / reset / check-email) renders a full-bleed
 * split-screen (navy editorial pitch + form), the same design as the senior
 * login — each page brings its own branding, so the layout is a passthrough.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
